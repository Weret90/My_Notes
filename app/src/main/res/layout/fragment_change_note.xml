<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".fragments.NoteChangeFragment">

    <EditText
        android:id="@+id/edit_note_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/default_margin"
        android:background="@android:color/holo_green_light"
        android:gravity="center"
        android:padding="@dimen/default_padding"
        android:textSize="@dimen/headline_text_size"
        android:textStyle="bold"
        tools:text="title" />

    <EditText
        android:id="@+id/edit_note_day_of_week"
        style="@style/note_description_default_style"
        tools:text="dayOfWeek" />

    <EditText
        android:id="@+id/edit_note_description"
        style="@style/note_description_default_style"
        tools:text="description" />

    <Button
        android:id="@+id/button_save_changes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/default_margin"
        android:text="@string/save_changes_of_note" />

</LinearLayout>